# Transcription Benchmark Fixtures

Put local-only transcription benchmark media here.

Recommended files:

- `typical.mp3` or another normal source file used in daily work.
- `long.mp3` or another longer source file for memory and timestamp stability checks.

Media in this folder is intentionally gitignored. Benchmark outputs are written to `.build/transcription-benchmark/`.
