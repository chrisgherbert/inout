# Website Assets

- `icon.png`: site/app icon used in header.
- `screenshot-placeholder.svg`: placeholder screenshot frame.

To use a real app screenshot:

1. Export your screenshot to PNG (recommended width 1600+).
2. Replace `screenshot-placeholder.svg` with your image (same filename), or update `website/index.html`.
